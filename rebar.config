%% deps dir
{deps_dir, ["deps"]}.

%% Поддиректории, в которые rebar должен заглядывать
{sub_dirs, ["rel", "apps/scribe_receiver"]}.

%% Опции компилятора
{erl_opts, [debug_info ,fail_on_warning]}.

%% 
{deps,
 [
%%	{thrift_erl, "0.5.0", {git, "git://github.com/xslogic/thrift_erl", "HEAD"}} 
 ]}.
